# ROI 配置工具使用说明

## 功能概述

ROI（Region of Interest）配置工具已集成到主 UI 中，支持快速配置各种识别区域的坐标。

## 支持的 ROI 类型

1. **姿势识别区域** (`posture_roi`) - 用于开镜时自动识别玩家姿势（站立/蹲下/趴下）
2. **枪械配件识别区域**：
   - 1号枪：名称、倍镜、枪口、握把、枪托
   - 2号枪：名称、倍镜、枪口、握把、枪托

## 使用方法

### 方法一：点击按钮

1. 在主界面底部找到 **"ROI 配置"** 按钮
2. 点击按钮打开 ROI 配置对话框
3. 从下拉列表选择要配置的 ROI 类型
4. 在截图上拖动鼠标框选区域
5. 点击 **"确定"** 保存配置

### 方法二：快捷键（推荐）

1. **按 F8 键**快速打开 ROI 配置对话框
2. 后续步骤同上

## 操作说明

- **框选区域**：按住鼠标左键拖动，绘制矩形框
- **缩放视图**：
  - 鼠标滚轮上下滚动
  - 或点击 +/- 按钮
- **确认保存**：按 Enter 键或点击"确定"按钮
- **取消退出**：按 Esc 或 Q 键，或点击"取消"按钮

## 实时反馈

- 框选时会显示当前坐标：`(left, top, right, bottom) [width x height]`
- 右上角显示当前鼠标位置：`X:xxx Y:yyy`
- 顶部显示当前已保存的 ROI 值
- 保存成功后会在日志区显示提示信息

## 配置文件

所有 ROI 配置保存在 `data/resolution_setting.py` 文件中，按分辨率分别存储。

示例：
```python
'3840x2160': {
    'Name_1': (86, 19, 350, 74),
    'Scope_1': (556, 56, 652, 146),
    ...
    'posture_roi': [1418, 1953, 1491, 2048],
},
```

## 注意事项

1. **分辨率适配**：不同分辨率需要分别配置 ROI
2. **最小尺寸**：框选区域至少 10x10 像素
3. **姿势识别**：建议选择包含角色下半身的区域，确保模板图片特征明显
4. **即时生效**：保存后配置立即生效，无需重启程序

## 常见问题

**Q: 为什么框选的区域不精确？**
A: 可以使用滚轮缩放视图，放大后更容易精确框选。

**Q: 配置错了怎么办？**
A: 重新打开 ROI 配置工具，选择对应的类型，重新框选即可覆盖旧配置。

**Q: 如何查看当前配置？**
A: 打开 ROI 配置对话框后，顶部会显示当前已保存的 ROI 值。

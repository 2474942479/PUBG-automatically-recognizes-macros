# ROI 配置工具使用说明

## 功能概述

ROI（Region of Interest）配置工具已集成到主 UI 中，支持快速配置各种识别区域的坐标。

## 支持的 ROI 类型

1. **姿势识别区域** (`posture_roi`) - 用于开镜时自动识别玩家姿势（站立/蹲下/趴下）
2. **枪械配件识别区域**：
   - 1号枪：名称、倍镜、枪口、握把、枪托
   - 2号枪：名称、倍镜、枪口、握把、枪托

## 使用方法

### 方法一：点击按钮

1. 在主界面底部找到 **"ROI 配置"** 按钮
2. 点击按钮打开 ROI 配置对话框
3. 从下拉列表选择要配置的 ROI 类型
4. 在 **1:1 全屏截图** 上拖动鼠标框选区域（图比窗口大时用 **滚动条** 移动查看，不缩放）
5. 点击 **「保存当前 ROI」** 将**当前选中的这一类型**写入配置文件（不会关闭窗口）
6. 全部类型调完后点击 **「关闭」** 退出

### 方法二：快捷键（推荐）

1. **按 F8 键**快速打开 ROI 配置对话框
2. 后续步骤同上

## 操作说明

- **框选区域**：按住鼠标左键拖动，绘制矩形框；可拖动/调整已存在的框
- **视图**：全屏截图为**原始像素 1:1 显示**，请用**滚动条**查看超出窗口的部分
- **保存到文件**：按 **Enter** 或点 **「保存当前 ROI」**（只保存当前类型）
- **Esc / Q**：**取消**正在画的新框、或拖动手势（**不会关闭**窗口）
- **关闭窗口**：点 **「关闭」**、点标题栏 **×**，或（在未改动的状态下）可依赖系统行为；**若有未通过「保存当前 ROI」落盘的修改**，关闭前会 **提示是否放弃**

## 实时反馈

- 框选时会显示当前坐标：`(left, top, right, bottom) [width x height]`
- 左下角会显示当前鼠标在截图上的原图像素位置：`X:xxx Y:yyy`
- 顶部会显示当前类型的 ROI 值
- 成功保存后，底部状态栏会提示已写入的类型与坐标，主程序日志区也会有相应记录

## 配置文件

所有 ROI 配置保存在 `data/resolution_setting.py` 文件中，按分辨率分别存储。

示例：
```python
'3840x2160': {
    'Name_1': (86, 19, 350, 74),
    'Scope_1': (556, 56, 652, 146),
    ...
    'posture_roi': [1418, 1953, 1491, 2048],
},
```

## 注意事项

1. **分辨率适配**：不同分辨率需要分别配置 ROI
2. **最小尺寸**：框选区域至少 10x10 像素
3. **姿势识别**：建议选择包含角色下半身的区域，确保模板图片特征明显
4. **即时生效**：通过「保存当前 ROI」写入后配置立即生效，无需重启程序

## 常见问题

**Q: 图太大框不准怎么办？**  
A: 使用 **滚动条** 在 1:1 原图上来回移动，按像素级框选；本工具不再对截图做放大/缩小，以保证坐标与识别一致。

**Q: 配置错了怎么办？**  
A: 重新打开 ROI 配置工具，选择对应类型，重新框选后点 **「保存当前 ROI」** 覆盖。

**Q: 如何查看当前配置？**  
A: 打开 ROI 配置对话框，顶部会显示当前类型下已填的 ROI 值；已写入文件的项以**最后一次成功保存**为准。

**Q: 点关闭时提示有未保存修改？**  
A: 表示与打开对话框时相比，有 ROI 被改过但**尚未**点过「保存当前 ROI」。选「是」会放弃未保存的改动并关闭；选「否」可继续调整并保存。

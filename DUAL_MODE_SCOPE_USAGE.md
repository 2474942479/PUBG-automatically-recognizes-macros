# 双模式倍镜使用说明

## 功能概述

支持具有两种模式的倍镜（如 `duobei1`/`duobei4`），可以在游戏中动态切换倍镜模式，无需重新识别。

## 支持的倍镜类型

- **duobei1**: 多倍镜(低倍模式) - 相当于红点/全息，灵敏度系数 1.0
- **duobei4**: 多倍镜(高倍模式) - 相当于4倍镜，灵敏度系数 4.0

## 使用方法

### 1. 自动识别
当按 `Tab` 键进行枪械识别时，如果检测到 `duobei` 倍镜：
- 默认初始化为 **低倍模式** (`duobei1`)
- 系统会自动跟踪当前倍镜的实际模式

### 2. 切换倍镜模式
**快捷键**: `Alt + 鼠标右键`

操作步骤：
1. 按住 `Alt` 键（左Alt或右Alt均可）
2. 点击鼠标右键
3. 倍镜会在低倍和高倍之间切换
4. 屏幕左下角会显示提示信息："倍镜已切换到XX模式"

### 3. 重置倍镜状态
**快捷键**: `Delete` 键

功能：
- 将所有枪械的倍镜模式重置为默认的**低倍模式**
- 不影响其他设置（枪械、姿态、压枪数据等）

> **注意**: `Insert` 键会重置所有数据（包括枪械、姿态、倍镜等），而 `Delete` 键只重置倍镜模式。

## 技术实现

### 状态管理
系统在 `ProcessClass` 中维护一个字典来跟踪每个枪械槽位的倍镜模式：
```python
self.duobei_scope_mode = {
    1: 'duobei1',  # 1号枪当前是低倍模式
    2: 'duobei4'   # 2号枪当前是高倍模式
}
```

### 核心方法

#### `toggle_duobei_scope(gun_slot=None)`
切换指定枪械的倍镜模式
- 参数: `gun_slot` - 枪械槽位 (1或2)，不指定则使用当前枪械
- 返回: 切换后的模式名称 ('duobei1' 或 'duobei4')

#### `reset_duobei_scope(gun_slot=None)`
重置倍镜模式到默认状态
- 参数: `gun_slot` - 枪械槽位，不指定则重置所有枪械

#### `get_current_scope()`
获取当前倍镜名称（已增强以支持双模式）
- 对于 `duobei` 倍镜，返回当前实际模式
- 对于其他倍镜，直接返回识别结果

### 按键监听

#### 键盘监听器 (`key_listener.py`)
- 监听 `Alt` 键的按下和释放状态
- 监听 `Delete` 键用于重置倍镜

#### 鼠标监听器 (`mouse_listener.py`)
- 检测 `Alt + 右键` 组合键
- 调用 `toggle_duobei_scope()` 切换倍镜模式
- 显示切换提示

## 配置说明

在 `data/fire_data.py` 中定义了倍镜的灵敏度系数：

```python
SCOPE_FACTOR = {
    "duobei1": 1.0,   # 低倍模式
    "duobei4": 4.0,   # 高倍模式
    # ... 其他倍镜
}
```

如果需要调整灵敏度系数，修改此配置即可。

## 工作流程示例

### 场景1: 单把枪使用双模式倍镜
1. 装备 M762 + duobei 倍镜
2. 按 `Tab` 识别 → 系统识别为 `duobei1`（低倍）
3. 按住 `Alt + 右键` → 切换到 `duobei4`（高倍）
4. 再次按 `Alt + 右键` → 切换回 `duobei1`（低倍）
5. 按 `Delete` → 重置为 `duobei1`（低倍）

### 场景2: 两把枪都使用双模式倍镜
1. 1号枪: M762 + duobei 倍镜
2. 2号枪: AKM + duobei 倍镜
3. 按 `Tab` 识别 → 两把枪都默认为 `duobei1`
4. 切换到1号枪，按 `Alt + 右键` → 1号枪变为 `duobei4`
5. 切换到2号枪，按 `Alt + 右键` → 2号枪变为 `duobei4`
6. 系统独立跟踪每把枪的倍镜模式

## 注意事项

1. **仅对 duobei 倍镜生效**: 只有识别为 `duobei1` 或 `duobei4` 的倍镜才支持切换
2. **独立状态**: 每把枪的倍镜模式是独立跟踪的，互不影响
3. **重置影响**: 
   - `Delete` 键只重置倍镜模式
   - `Insert` 键重置所有数据（包括倍镜模式）
4. **识别优先级**: 每次按 `Tab` 重新识别后，duobei 倍镜会重置为默认的 `duobei1` 模式

## 故障排除

### 问题1: Alt + 右键没有反应
**可能原因**:
- 当前枪械没有装备 duobei 倍镜
- 还没有进行过枪械识别

**解决方法**:
- 确认倍镜类型是否为 duobei
- 先按 `Tab` 进行识别

### 问题2: 切换后压枪效果不对
**可能原因**:
- 倍镜模式与实际不符

**解决方法**:
- 按 `Delete` 重置倍镜模式
- 或再次按 `Alt + 右键` 切换到正确模式

### 问题3: 想查看当前倍镜模式
**解决方法**:
- 观察 HUD 显示的倍镜名称
- 或查看日志输出中的切换提示

## 扩展建议

如果需要支持更多双模式倍镜，可以：
1. 在 `data/fire_data.py` 中添加新的倍镜类型
2. 在 `KEY_DATA_V3["Scope"]` 中定义编码映射
3. 在 `SCOPE_FACTOR` 中设置灵敏度系数
4. 确保倍镜名称包含 `duobei` 前缀以启用自动切换功能
